# Everything EDA.py imports. Lower bounds only — the script uses no APIs that
# have changed recently, so newer versions are fine.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
